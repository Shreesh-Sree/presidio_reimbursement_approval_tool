# AI review service

AWS-hosted advisory service for minimized reimbursement-risk snapshots. It never approves, rejects, pays, or accesses Neon directly. Configure Groq only through AWS Secrets Manager.

```bash
uv sync
uv run pytest -q
```
