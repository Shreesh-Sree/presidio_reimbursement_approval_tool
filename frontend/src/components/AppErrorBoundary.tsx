import { Component, type ErrorInfo, type ReactNode } from "react";
type Props = { children: ReactNode }; type State = { error: Error | null };
export class AppErrorBoundary extends Component<Props, State> { state: State = { error: null }; static getDerivedStateFromError(error: Error) { return { error }; } componentDidCatch(error: Error, info: ErrorInfo) { console.error(error, info); } render() { if (!this.state.error) return this.props.children; return <main className="repl-page"><p className="repl-eyebrow">Something went wrong</p><h1 className="repl-title">Let’s start<br />again.</h1><p className="repl-lede">{this.state.error.message}</p><button className="signout" onClick={() => this.setState({ error: null })}>Try again</button></main>; } }
