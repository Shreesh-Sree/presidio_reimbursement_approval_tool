import { useAuth } from "../../auth/AuthContext";
export function AccessDeniedPage() { const { deniedEmail, logout } = useAuth(); return <main className="repl-page"><p className="repl-eyebrow">Access required</p><h1 className="repl-title">You don't have access yet</h1><div className="repl-card" style={{ maxWidth: 620, padding: 32 }}><p className="repl-lede">Your account {deniedEmail ? `(${deniedEmail}) ` : ""}is signed in, but it has not been granted access to this organization’s reimbursement workspace.</p><button className="signout" onClick={() => void logout()}>Sign out</button></div></main>; }
